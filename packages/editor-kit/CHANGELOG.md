# Changelog

## 0.1.0 — 2026-08-23

- Publish the companion package as `@velarscript-labs/editor-kit`, outside the
  Standard/toolchain scope.
- Add the editor-neutral VelarScript language-service descriptor.
- Add protocol 4/5 compatibility checks against the server initialize result.
- Add project-local `velar` executable resolution through the `node` subpath.
